version: "3.7"

services:
  server:
    image: homeassistant/home-assistant@sha256:6635f0a88b19c8cfe02de897c6a6806aded2b538796d60f6f7aee15b9e0c78b4
    network_mode: host
    # UI at default port 8123
    privileged: true
    volumes:
      - ${APP_DATA_DIR}/data:/config
      - ${UMBREL_ROOT}/data/storage/downloads:/media
      - /dev:/dev
